version: 1
frontend:
  phases:
    preBuild:
      commands:
        - dotnet restore
    build:
      commands:
        - dotnet publish -c Release -o dist
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - ~/.nuget/**/*